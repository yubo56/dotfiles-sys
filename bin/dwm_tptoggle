#! /bin/zsh
. ~/.shell_envs
touchpad_name='PNP0C50:0b 36B6:C001 Touchpad'
abcd=$(xinput list-props $touchpad_name | grep 'Device Enabled')
echo $abcd
if [[ $abcd =~ ':.*0' ]]; then
    echo 'enabling'
    xinput enable $touchpad_name
else
    echo 'disabling'
    xinput disable $touchpad_name
fi
