#! /bin/zsh
. ~/.shell_envs
abcd=$(xinput list-props $xinput_touchpad_idx | grep 'Device Enabled')
echo $abcd
if [[ $abcd =~ ':.*0' ]]; then
    echo 'enabling'
    xinput enable $xinput_touchpad_idx
else
    echo 'disabling'
    xinput disable $xinput_touchpad_idx
fi
