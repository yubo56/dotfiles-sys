#!/bin/bash

if [[ $(setxkbmap -query | grep 'variant') == "" ]]; then
    suffix=U
elif [[ $(setxkbmap -query | grep 'variant') =~ "dvorak-l" ]]; then
    suffix=L
else
    suffix=D
fi
xsetroot -name "$(cat /tmp/status)$(/home/yssu/bin/dateWeek)$(date +'%m/%d/%y %H:%M:%S')$suffix"
